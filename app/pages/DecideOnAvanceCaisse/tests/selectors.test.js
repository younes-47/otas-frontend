// import { selectDecideOnAvanceCaisseDomain } from '../selectors';

describe('selectDecideOnAvanceCaisseDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
