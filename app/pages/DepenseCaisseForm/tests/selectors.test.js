// import { selectDepenseCaisseFormDomain } from '../selectors';

describe('selectDepenseCaisseFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
