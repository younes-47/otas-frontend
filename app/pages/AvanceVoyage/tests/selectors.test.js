// import { selectAvanceVoyageDomain } from '../selectors';

describe('selectAvanceVoyageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
