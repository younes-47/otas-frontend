// import { selectUnauthorizedDomain } from '../selectors';

describe('selectUnauthorizedDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
