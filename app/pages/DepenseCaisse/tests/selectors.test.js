// import { selectDepenseCaisseDomain } from '../selectors';

describe('selectDepenseCaisseDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
