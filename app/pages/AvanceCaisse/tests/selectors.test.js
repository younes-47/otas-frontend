// import { selectAvanceCaisseDomain } from '../selectors';

describe('selectAvanceCaisseDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
