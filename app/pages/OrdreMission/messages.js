/*
 * OrdreMission Messages
 *
 * This contains all the text for the OrdreMission container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OrdreMission';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrdreMission container!',
  },
});
