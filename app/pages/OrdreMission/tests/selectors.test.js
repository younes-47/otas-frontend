// import { selectOrdreMissionDomain } from '../selectors';

describe('selectOrdreMissionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
