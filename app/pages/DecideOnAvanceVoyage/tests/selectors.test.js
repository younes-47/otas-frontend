// import { selectDecideOnAvanceVoyageDomain } from '../selectors';

describe('selectDecideOnAvanceVoyageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
