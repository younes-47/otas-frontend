// import { selectMinimalAccessDomain } from '../selectors';

describe('selectMinimalAccessDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
