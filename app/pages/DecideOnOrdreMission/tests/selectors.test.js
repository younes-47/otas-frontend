// import { selectDecideOnOrdreMissionDomain } from '../selectors';

describe('selectDecideOnOrdreMissionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
