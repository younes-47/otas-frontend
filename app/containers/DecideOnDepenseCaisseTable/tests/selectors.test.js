// import { selectDecideOnDepenseCaisseTableDomain } from '../selectors';

describe('selectDecideOnDepenseCaisseTableDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
