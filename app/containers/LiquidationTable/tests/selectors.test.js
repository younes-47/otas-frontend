// import { selectLiquidationTableDomain } from '../selectors';

describe('selectLiquidationTableDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
