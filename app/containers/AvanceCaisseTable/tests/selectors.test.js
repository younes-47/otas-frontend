// import { selectAvanceCaisseTableDomain } from '../selectors';

describe('selectAvanceCaisseTableDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
