// import { selectDecideOnDepenseCaisseFormDomain } from '../selectors';

describe('selectDecideOnDepenseCaisseFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
