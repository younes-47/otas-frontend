/*
 * Azerty Messages
 *
 * This contains all the text for the Azerty container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Azerty';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Azerty container!',
  },
});
