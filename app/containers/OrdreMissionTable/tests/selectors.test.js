// import { selectOrdreMissionTableDomain } from '../selectors';

describe('selectOrdreMissionTableDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
