// import { selectDecideOnAvanceVoyageTableDomain } from '../selectors';

describe('selectDecideOnAvanceVoyageTableDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
