// import { selectDepenseCaisseTableDomain } from '../selectors';

describe('selectDepenseCaisseTableDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
