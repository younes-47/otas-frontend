// import { selectAvanceVoyageViewDomain } from '../selectors';

describe('selectAvanceVoyageViewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
