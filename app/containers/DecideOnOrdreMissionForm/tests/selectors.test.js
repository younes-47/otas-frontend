// import { selectDecideOnOrdreMissionFormDomain } from '../selectors';

describe('selectDecideOnOrdreMissionFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
