// import { selectDecideOnLiquidationTableDomain } from '../selectors';

describe('selectDecideOnLiquidationTableDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
