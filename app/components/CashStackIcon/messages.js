/*
 * CashStackIcon Messages
 *
 * This contains all the text for the CashStackIcon component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CashStackIcon';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CashStackIcon component!',
  },
});
