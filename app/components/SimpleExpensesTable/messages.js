/*
 * SimpleExpensesTable Messages
 *
 * This contains all the text for the SimpleExpensesTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SimpleExpensesTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SimpleExpensesTable component!',
  },
});
