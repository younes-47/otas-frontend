/*
 * FileUploadForm Messages
 *
 * This contains all the text for the FileUploadForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FileUploadForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FileUploadForm component!',
  },
});
