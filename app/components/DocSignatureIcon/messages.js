/*
 * DocSignatureIcon Messages
 *
 * This contains all the text for the DocSignatureIcon component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DocSignatureIcon';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DocSignatureIcon component!',
  },
});
