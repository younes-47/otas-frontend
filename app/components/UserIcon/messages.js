/*
 * UserIcon Messages
 *
 * This contains all the text for the UserIcon component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.UserIcon';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserIcon component!',
  },
});
