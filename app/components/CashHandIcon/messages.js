/*
 * CashHandIcon Messages
 *
 * This contains all the text for the CashHandIcon component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CashHandIcon';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CashHandIcon component!',
  },
});
